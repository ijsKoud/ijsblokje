{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"build:watch": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"],
			"persistent": true
		},
		"lint": {
			"outputs": []
		}
	}
}
